import React,{useState} from 'react';import { useTranslation } from 'react-i18next';import { Link } from 'react-router-dom';export default function Calculator(){const { t } = useTranslation();const [cadastral,setCadastral]=useState('');const [rate,setRate]=useState('0.6');const [discount,setDiscount]=useState('0');const parsed=v=>parseFloat((v||'').toString().replace(',','.'))||0;const estimate=()=>{const base=parsed(cadastral);const r=parsed(rate)/100;const d=parsed(discount)/100;const gross=base*r;const net=gross*(1-d);return Math.max(0,Math.round(net*100)/100)};return(<div className='min-h-screen bg-onyx text-white p-6 flex items-center justify-center'><div className='w-full max-w-xl bg-gray-900 rounded-2xl p-6 shadow-soft'><div className='mb-4'><Link to='/' className='text-gold'>&larr; {t('back')}</Link></div><h2 className='text-2xl font-semibold text-gold mb-2'>{t('calculator_title')}</h2><p className='text-gray-300 mb-6'>{t('calculator_desc')}</p><div className='space-y-4'><div><label className='block text-sm mb-1'>{t('input_cadastral')}</label><input value={cadastral} onChange={e=>setCadastral(e.target.value)} className='w-full p-3 rounded bg-gray-800 border border-gray-700' placeholder='e.g. 120000' /></div><div><label className='block text-sm mb-1'>{t('input_rate')}</label><input value={rate} onChange={e=>setRate(e.target.value)} className='w-full p-3 rounded bg-gray-800 border border-gray-700' placeholder='0.6' /></div><div><label className='block text-sm mb-1'>{t('input_discount')}</label><input value={discount} onChange={e=>setDiscount(e.target.value)} className='w-full p-3 rounded bg-gray-800 border border-gray-700' placeholder='0' /></div><div className='mt-4 text-xl'>{t('result_estimate')}: <span className='text-gold'>{estimate().toFixed(2)}</span></div></div></div></div>);}