const { generateRegistrationOptions } = require('@simplewebauthn/server');module.exports=(req,res)=>{const userId=req.body?.userId;if(!userId)return res.status(400).json({error:'userId required'});const options=generateRegistrationOptions({rpName:process.env.RP_NAME||'MIBI',rpID:process.env.RP_ID||'localhost',userID:userId,userName:userId,attestationType:'none',authenticatorSelection:{userVerification:'preferred'}});res.json(options);}