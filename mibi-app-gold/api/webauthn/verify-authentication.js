const { verifyAuthenticationResponse } = require('@simplewebauthn/server');module.exports=async(req,res)=>{try{const {credential,expectedChallenge}=req.body||{};const verification=await verifyAuthenticationResponse({credential,expectedChallenge,expectedRPID:process.env.RP_ID||'localhost',expectedOrigin:process.env.ORIGIN||'http://localhost:5173'});res.json(verification);}catch(e){res.status(400).json({error:e.message});}}