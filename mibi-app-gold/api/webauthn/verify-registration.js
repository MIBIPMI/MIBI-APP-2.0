const { verifyRegistrationResponse } = require('@simplewebauthn/server');module.exports=async(req,res)=>{try{const {credential,expectedChallenge}=req.body||{};const verification=await verifyRegistrationResponse({credential,expectedChallenge,expectedOrigin:process.env.ORIGIN||'http://localhost:5173',expectedRPID:process.env.RP_ID||'localhost'});res.json(verification);}catch(e){res.status(400).json({error:e.message});}}